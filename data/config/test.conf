input { 
  file {
    path => "/Users/jojo/PycharmProjects/lambda/ch16_cgh_500000/chimp.16.500000.processed"
    start_position => "beginning"
  }
}

filter {
  grok {
    match => { "message" => "%{WORD:sp} %{WORD:chr} %{NONNEGINT:loc} %{GREEDYDATA:data}" }
  }
}

output {
  elasticsearch { 
    hosts => ["localhost:9200"] 
    index => "test16"
  }
}
